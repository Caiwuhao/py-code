# Reconstructed for DFG_simu and validated against the project's saved CSV results.
name: my_env
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
      - numpy==2.3.5
      - scipy==1.17.0
      - pandas==2.2.3
      - matplotlib==3.10.8
